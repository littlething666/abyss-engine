/**
 * Status mapper — Phase 3.6 Step 7.
 *
 * @deprecated Import from `../contracts/statusMapper` instead.
 * This re-export exists for backward compatibility during migration.
 */
export {
  dbStatusToTransport,
  transportStatusToDb,
  DB_TO_TRANSPORT_STATUS,
  ACTIVE_TRANSPORT_STATUSES,
  TERMINAL_TRANSPORT_STATUSES,
  type TransportRunStatus,
} from '../contracts/statusMapper';
