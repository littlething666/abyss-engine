/**
 * Canonical list of NEXT_PUBLIC_* variables exported during Next.js build.
 * Keep this list as the single source of truth for workflow env wiring.
 */
export const NEXT_PUBLIC_BUILD_ENV_KEYS = [
  'NEXT_PUBLIC_DURABLE_GENERATION_URL',
  'NEXT_PUBLIC_DURABLE_RUNS',
  'NEXT_PUBLIC_LLM_WORKER_URL',
  'NEXT_PUBLIC_POSTHOG_TOKEN',
];
